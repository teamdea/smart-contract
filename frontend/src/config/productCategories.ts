// Mirrors backend/src/config/constants.ts's PRODUCT_CATEGORIES exactly -
// keep both in sync. A fixed list (not free text) so a Buyer's category
// filter always matches a Supplier's chosen category exactly.
export const PRODUCT_CATEGORIES = ["Automobile", "Electronics", "Textiles", "Machinery", "Agriculture"];
